                            Table "public.agents"
     Column      |           Type           | Collation | Nullable | Default 
-----------------+--------------------------+-----------+----------+---------
 id              | character varying(50)    |           | not null | 
 workspace_id    | character varying(50)    |           | not null | 
 name            | character varying(100)   |           | not null | 
 voice_id        | character varying(50)    |           |          | 
 language        | character varying(10)    |           |          | 
 prompt_template | text                     |           |          | 
 welcome_message | text                     |           |          | 
 is_orchestrator | boolean                  |           |          | 
 description     | text                     |           |          | 
 is_active       | boolean                  |           |          | 
 settings        | json                     |           |          | 
 created_at      | timestamp with time zone |           |          | now()
 updated_at      | timestamp with time zone |           |          | now()
Indexes:
    "agents_pkey" PRIMARY KEY, btree (id)
    "ix_agents_id" btree (id)
Foreign-key constraints:
    "agents_workspace_id_fkey" FOREIGN KEY (workspace_id) REFERENCES workspaces(id)
Referenced by:
    TABLE "communications" CONSTRAINT "communications_agent_id_fkey" FOREIGN KEY (agent_id) REFERENCES agents(id)
    TABLE "integrations" CONSTRAINT "integrations_agent_id_fkey" FOREIGN KEY (agent_id) REFERENCES agents(id)
    TABLE "phone_numbers" CONSTRAINT "phone_numbers_agent_id_fkey" FOREIGN KEY (agent_id) REFERENCES agents(id)

